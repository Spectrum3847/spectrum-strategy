{
  "name": "spectrumstrategy-cron-worker",
  "description": "Cloudflare Worker running SpectrumStrategy's crons on a five-minute external trigger: shift DMs, accuracy alerts, report issues, the schedule mirror and the usage rollup",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "deploy": "pnpm dlx wrangler@latest deploy",
    "dev": "pnpm dlx wrangler@latest dev"
  },
  "engines": {
    "node": "24"
  },
  "packageManager": "pnpm@12.5.1",
  "private": true
}
